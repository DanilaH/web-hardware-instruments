import{describe,expect,it}from'vitest';import{createDoubleClickState,observeButtonPress}from'./double-click-measurement';
describe('double click measurement',()=>{it('measures same-button intervals and isolates different buttons',()=>{let s=createDoubleClickState();s=observeButtonPress(s,0,100);s=observeButtonPress(s,1,110);s=observeButtonPress(s,0,180);expect(s.lastGapMs).toBe(80);expect(s.rapidRepeatEvents).toBe(0);expect(s.totalPresses).toBe(3);});it('flags the exact 50ms boundary',()=>{let s=createDoubleClickState();s=observeButtonPress(s,0,100);s=observeButtonPress(s,0,150);expect(s.rapidRepeatEvents).toBe(1);});it('does not flag 51ms',()=>{let s=createDoubleClickState();s=observeButtonPress(s,0,100);s=observeButtonPress(s,0,151);expect(s.rapidRepeatEvents).toBe(0);});it('ignores a non-positive gap without moving the valid interval baseline',()=>{let s=createDoubleClickState();s=observeButtonPress(s,0,100);s=observeButtonPress(s,0,100);s=observeButtonPress(s,0,150);expect(s.lastGapMs).toBe(50);expect(s.rapidRepeatEvents).toBe(1);});it('ignores non-finite timestamps completely',()=>expect(observeButtonPress(createDoubleClickState(),0,Number.NaN)).toEqual(createDoubleClickState()));it('reset starts with empty timestamps and metrics',()=>expect(createDoubleClickState()).toEqual({previousByButton:[null,null,null,null,null],totalPresses:0,rapidRepeatEvents:0,shortestGapMs:null,lastGapMs:null}));});
